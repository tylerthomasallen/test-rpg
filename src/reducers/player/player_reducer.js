const initialPlayer = {
    position: [0, 0]
}

const playerReducer = (state = initialPlayer, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default playerReducer;